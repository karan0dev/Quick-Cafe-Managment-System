{
  "name": "quickcafe-mongodb-atlas",
  "version": "2.0.0",
  "private": true,
  "description": "QuickCafe static frontend with a Node.js API backed by MongoDB Atlas.",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "dev": "node server/server.js",
    "seed": "node server/seed.js",
    "check": "node --check js/script.js && node --check js/admin.js && node --check server/server.js && node --check server/seed.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongodb": "^6.8.0"
  }
}
